services:
  app:
    build: .
    ports:
      - "80:80"
    volumes:
      - .:/app
    environment:
      - VITE_API_PATH=${VITE_API_PATH}
      - VITE_SPRINGSECURITY_USERNAME=admin
      - VITE_SPRINGSECURITY_PASSWORD=L12345
      
