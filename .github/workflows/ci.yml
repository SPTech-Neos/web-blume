name: CI WEB NEOS

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubunru-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: build
        run: |
          docker-compose -f docker-compose.yml build
      - name: publish
        run: |
          docker push victorlcdpaula/web-blume:latest